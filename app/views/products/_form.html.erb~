 <%= form_for @product, :html => { :class => 'well form-inline'} do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class=" well product_inputs">
    
    <%= f.select :category, options_for_select([["Type-Wood", "0"], ["Type-Plywood","1"],["Type-Bedding","2"]]), { :selected => "mm" }, { :id => "select_category" } %>

    <%= f.text_field :code, :placeholder => "Code", :class => "input-small" %>
 
    <%= f.text_field :name, :placeholder => "Name", :class => "input-small" %>

    <%= f.text_field :size1, :placeholder => "Dimensions", :class => "input-small" %>
    
    <%= f.text_field :size2, :placeholder => "Dimensions", :class => "input-small" %>
  
    <%= f.fields_for :stocks do |fi| %>
     <%= render 'stock_fields', :f => fi %>
    <% end %>

   </div> 
  </br></br>  </br></br>
  <div class="actions">
    
<%= link_to 'Back', products_path, :class => "btn info" %>
<%= f.submit 'Create', :class => "btn-primary" %>
  </div>

<% end %>

